# Core
fastapi>=0.109.0
uvicorn[standard]>=0.27.0

# Database
pyodbc>=5.0.1

# Validation / Settings
pydantic>=2.5.0
pydantic-settings>=2.1.0

# Scheduling
apscheduler>=3.10.0

# HTTP client (collectors)
requests>=2.31.0
urllib3>=2.1.0

# Auth (Phase 3 — wired but not enforced yet)
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
python-multipart>=0.0.9
